{
  "name": "p29-vitest-import-example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.19.0"
  },
  "scripts": {
    "generate": "node scripts/generate-fixture.mjs",
    "variant:baseline": "node scripts/set-variant.mjs baseline",
    "variant:candidate": "node scripts/set-variant.mjs candidate",
    "test:baseline": "npm run variant:baseline && vitest run --config vitest.config.ts --coverage --reporter=default --reporter=json --outputFile.json=results/current.json",
    "test:candidate": "npm run variant:candidate && vitest run --config vitest.config.ts --coverage --reporter=default --reporter=json --outputFile.json=results/current.json",
    "verify:manifest": "node scripts/verify-manifest.mjs",
    "measure": "node scripts/measure.mjs",
    "failure-check": "node scripts/failure-check.mjs"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "5.0.1",
    "typescript": "5.9.2",
    "vitest": "5.0.1"
  }
}
